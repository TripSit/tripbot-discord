{
  "name": "@tripsit/tripbot-discord",
  "version": "0.1.0",
  "private": true,
  "main": "./dist/index.js",
  "scripts": {
    "start": "NODE_ENV=production node ./dist",
    "build": "npm run clean && tsc",
    "clean": "rm -rf dist",
    "dev": "NODE_ENV=development nodemon -w bot/**/* -e ts,json --exec 'node --inspect -r ts-node/register ./bot/index.ts'",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "SevenCats <sevencats@tripsit.me>",
  "license": "MIT",
  "dependencies": {
    "discord.js": "^12.5.3",
    "knex": "^0.95.6",
    "knex-stringcase": "^1.4.5"
  },
  "devDependencies": {
    "@types/knex": "^0.16.1",
    "@types/node": "^15.12.2",
    "nodemon": "^2.0.7",
    "ts-node": "^10.0.0",
    "typescript": "^4.3.2"
  }
}
